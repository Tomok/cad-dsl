struct Point {
    x: i32,
    y: i32,
}

struct Sketch {
    container entities,
}

let sketch: Sketch;

with sketch {
    let .p1: Point;
    let .p2: Point;
    .p1.x == 0;
    .p1.y == 0;
    .p2.x == .p1.x + 10;
    .p2.y == .p1.y + 10;
}
